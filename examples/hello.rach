# Smallest useful Rach program: write, read, check, delete.
# No `main`, no `import`, no end markers — top-level statements run in order.

write("/tmp/rach_hello.txt", "hello from Rach!")
read("/tmp/rach_hello.txt")
exists("/tmp/rach_hello.txt")
del("/tmp/rach_hello.txt")
