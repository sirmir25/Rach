# OS-aware install with the bash DSL + ai_generate fallback.

if linux:
    bash = generate install oh my zsh
    ai_generate(language="bash", task="установить oh-my-zsh на Linux")
if macos:
    bash = generate install oh my zsh
    ai_generate(language="bash", task="установить oh-my-zsh на macOS")
